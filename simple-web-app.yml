server:
  port: 8082

app:
  config:
    a: jsdsd
    b: asdas
    x: 09867

feign:
  client:
    jsonplaceholder:
      url: https://jsonplaceholder.typicode.com
      connectTimeout: 20
      readTimeout: 3000
      headers:
        X-Custom-Header: "other-header"
        Content-Type: "application/json"